# Security

- پنل مدیریت به‌صورت پیش‌فرض فقط روی `127.0.0.1` اجرا می‌شود.
- Proxy نیز پیش‌فرض فقط Local است؛ برای LAN باید گزینه مربوطه آگاهانه فعال شود.
- credentialهای Cloudflare از مسیر استاندارد `~/.cloudflared` خوانده می‌شوند و داخل repository قرار نمی‌گیرند.
- هر پروژه با سطح دسترسی همان User ویندوز/لینوکس اجرا می‌شود؛ دستور پروژه را فقط از پروژه‌های قابل اعتماد ثبت کنید.
- برای دسترسی عمومی از Cloudflare Tunnel استفاده کنید و Admin Panel را روی دامنه عمومی Route نکنید.
